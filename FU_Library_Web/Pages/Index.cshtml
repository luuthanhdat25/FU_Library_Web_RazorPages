@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="container mt-5">
    <div class="text-center">
        <form class="d-flex justify-content-center" method="get" action="/SearchBook" onsubmit="return validateForm()">
            <select class="form-select w-auto" name="options">
                <option value="0" selected>All</option>
                <option value="1">Title</option>
                <option value="2">Author</option>
                <option value="3">Publisher</option>
            </select>
            <input class="form-control mx-2" type="search" name="keyword" placeholder="Everything" aria-label="Search">
            <button class="btn btn-primary" type="submit">Search</button>
        </form>

        <div class="mt-4 d-flex align-items-center">
            <label for="librarySelect" class="form-label fw-bold me-2 mb-0">Limit search <All library></label>
            <select class="form-select w-auto" id="librarySelect" name="libraryOptions">
                <option value="all" selected>All Libraries</option>
                <option value="library1">Library 1</option>
                <option value="library2">Library 2</option>
                <option value="library3">Library 3</option>
                <option value="library4">Library 4</option>
            </select>
        </div>

        <div class="row row-cols-1 row-cols-md-2 g-4 mt-2">
            @* @foreach (var book in Model.Books)
            {
                <div class="col">
                    <div class="card h-100">
                        <img src="@(string.IsNullOrEmpty(book.ImageUrl) ? "https://via.placeholder.com/150" : book.ImageUrl)" class="card-img-top" alt="@book.Title" />
                        <div class="card-body">
                            <h5 class="card-title">@book.Title</h5>
                            <p class="card-text">Thông tin xuất bản: @book.PublicationInfo</p>
                            <p class="card-text">Mã xếp giá: @book.ClassificationCode</p>
                            <p class="card-text">Mã ĐKCB: @book.RegisterCode</p>
                        </div>
                    </div>
                </div>
            } *@
            <div class="col">
                <div class="card h-100">
                    <div class="row g-0">
                        <div class="col-md-4">
                            <img src="https://library.fpt.edu.vn/Uploads/HN/Images/Catalogue/FPT240040652.jpg" : book.ImageUrl)" class="card-img-top" class="img-zoom rounded-start" />
                        </div>
                        <div class="col-md-8">
                            <div class="card-body text-start">
                                <h5 class="card-title">
                                    Giáo trình lịch sử Đảng Cộng sản Việt Nam : Dành cho bậc đại học hệ không chuyên lý luận chính trị / Bộ Giáo dục và Đào tạo
                                </h5>
                                <p class="card-text">Thông tin xuất bản: </p>
                                <p class="card-text">Mã xếp giá: </p>
                                <p class="card-text">Mã ĐKCB: </p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
